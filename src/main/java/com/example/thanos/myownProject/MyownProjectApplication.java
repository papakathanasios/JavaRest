package com.example.thanos.myownProject;

import com.example.thanos.myownProject.SocialNetwork.SocialNetworkService;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class MyownProjectApplication {

	public static void main(String[] args) {
		SpringApplication.run(MyownProjectApplication.class, args);
	}

}
